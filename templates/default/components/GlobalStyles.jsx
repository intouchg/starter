import { Global } from '@intouchg/components'

const GlobalStyles = () => (
	<Global
		reset
		// style={`
		// 	:root {
		// 		--color-red: #ff0000;
		// 	}
		// `}
	/>
)

export default GlobalStyles
